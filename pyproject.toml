[project]
name = "backend"
version = "0.1.0"
description = "A backend for managing API and data storage to database"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "supabase>=2.0.0",
    "python-dotenv>=1.0.0",
]
